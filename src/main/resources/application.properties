spring.application.name=travel-project
server.port=8080

# MySQL ?? (??)
spring.datasource.url=jdbc:mysql://localhost:3306/travel_db?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=apple
spring.datasource.password=apple
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.configuration.map-underscore-to-camel-case=true

# Thymeleaf ?? ??
spring.thymeleaf.cache=false

